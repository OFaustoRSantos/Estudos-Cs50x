#include <stdio.h>
#include <cs50.h>

int main(void)
{
    /* - Part 1 - Operators and variables
    // name
    string name = get_string("What is your name? ");
    // age
    int age = get_int("What's your age? ");
    string girlfriend = get_string("What is your girlfriend name? ");
    printf("Hello %s, %i, your girlfriend is %s\n", name, age, girlfriend);
    */

    /* - Part 2 - If, and loops
    for (int i = 0; i < 2; i++)
    {
        printf("%i\n" i);
    }
    #or:
    int i = 0;
    while (i < 2)
    {
     printf("%i\n", i);
     i = i + 1;
    }
    */

    /* - Mario challenge
        int height = get_int("How big will be the pyramid?" );

    for(int i = 0; i < height + 1; i++)
    {
        for(int x = 1; x < i +1; x++)
        {
            printf("#");
        }
        printf("\n");
    }
    printf("that's your pyramid\n");
    
    */
   
    
}